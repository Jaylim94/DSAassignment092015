CREATE TABLE "QUESTION"
(    
   "QUESTIONID" INT not null primary key
        GENERATED ALWAYS AS IDENTITY
        (START WITH 1, INCREMENT BY 1),   
    "QUESTION" varchar(100) NOT NULL,
    "ANSWER" INT not null,
    "ANSWERSTATEMENT" varchar(100) NOT NULL
);